{
  "installCommand": "if [ -d 'Frontend/vite-project' ]; then cd Frontend/vite-project && npm install; else npm install; fi",
  "buildCommand": "if [ -d 'Frontend/vite-project' ]; then cd Frontend/vite-project && npm run build; else npm run build; fi",
  "outputDirectory": "Frontend/vite-project/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
