{
  "name": "docker-aws-root",
  "private": true,
  "scripts": {
    "dev": "if [ -d 'Frontend/vite-project' ]; then cd Frontend/vite-project && npm run dev; else npm run dev; fi",
    "build": "if [ -d 'Frontend/vite-project' ]; then cd Frontend/vite-project && npm run build; else npm run build; fi"
  }
}
